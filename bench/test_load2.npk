pub func:failsafe = int32(tbb32:err) { exit 1i32; };
pub func:main = int32(int32:argc, wild int8->:argv) {
    wild int8->:ptr = nodrop alloc(32i64);
    int64:val = @cast_unchecked<int64>(<-@cast_unchecked<int64->>(ptr));
    dalloc(ptr);
    return 0i32;
};
