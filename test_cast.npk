pub func:failsafe = int32(tbb32:err) { exit 1i32; };
pub func:main = int32(int32:argc, wild int8->:argv) {
    wild int8->:ptr = nodrop alloc(32768i64);
    int64:addr = @cast_unchecked<int64>(ptr);
    dalloc(ptr);
    if (addr < 0i64) {
        return 123i32;
    }
    return 0i32;
};
